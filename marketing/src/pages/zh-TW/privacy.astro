---
import LegalDocument from "../../components/LegalDocument.astro";
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getMessages } from "../../i18n";
import { privacyPolicyZhTw } from "@legal/privacy-policy-zh-TW";

const messages = getMessages("zh-TW");
---

<BaseLayout
  title={messages.legal.privacyTitle}
  description={messages.meta.privacyDescription}
  locale="zh-TW"
  messages={messages}
  pathname="/zh-TW/privacy"
>
  <main class="container" style="padding-block: 2.5rem 4rem">
    <p style="margin: 0 0 1rem; font-size: 0.875rem">
      <a href="/zh-TW" class="muted" style="text-decoration: none">{messages.legal.homeLink}</a>
    </p>
    <h1 style="margin: 0; font-size: 1.75rem; font-weight: 700">{messages.legal.privacyTitle}</h1>
    <div style="margin-top: 1.5rem">
      <LegalDocument
        doc={privacyPolicyZhTw}
        effectiveDateLabel={messages.legal.effectiveDateLabel}
      />
    </div>
  </main>
</BaseLayout>
