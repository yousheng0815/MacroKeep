/** Production app origin — change for local app testing if needed. */
export const APP_ORIGIN = "https://app.macrokeep.com";

export const APP_URL = `${APP_ORIGIN}/`;
export const FEEDBACK_EMAIL = "feedback@macrokeep.com";
