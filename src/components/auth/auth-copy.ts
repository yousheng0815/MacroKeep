/** Heading when the user is signed in but must grant Drive app-data scope before the app can load. */
export const GOOGLE_DRIVE_APP_DATA_CONSENT_TITLE = "Allow Google Drive access";

/** Shown on login / auth gate when explaining Drive app-data scope. */
export const GOOGLE_DRIVE_APP_DATA_BLURB =
  "We need Google Drive permission to save your data. It's limited to OpenMacro's own app folder on Drive. We can't see your other files.";
